framework:
  secret: fos
  router:
    resource: "%kernel.root_dir%/fixtures/config/routing.yml"
  test: true
  session:
    enabled: false
  validation: { enable_annotations: true }

fos_rest:
    body_converter:
        enabled: true

draw_swagger:
    schema:
        info:
            title: "test"